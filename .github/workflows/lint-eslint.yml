name: Lint eslint

on:
  pull_request:
  push:
    branches:
      - master

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: lint-eslint-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  lint-eslint:
    uses: chenasraf/workflows/.github/workflows/nextcloud-lint-eslint.yml@nextcloud-latest
    secrets: inherit
    with:
      # Runs whatever changed. A filtered-out job reports success, so a commit
      # touching nothing it watches would paint over the previous commit's red.
      path-filters: |
        - '**'
